import './_group.css';
import { ArrowRight, BarChart3, CalendarDays, Clock3, Target, TrendingUp, Users } from 'lucide-react';
import { AppointmentRow, Button, dashboard, Head, MetricCard, Shell } from './_shared';

export function CurrentOverview() {
  const max = Math.max(...dashboard.stageCounts.map((s) => s.count));
  return <Shell active="Overview"><div className="hairline-grid min-h-[calc(100vh-76px)] px-5 py-8 md:px-9 md:py-10"><div className="mx-auto max-w-[1400px]"><Head eyebrow="Tuesday, 14 April 2026 · Johannesburg" title="Welcome back, Jordan." detail="The revenue desk is clear on the next best move. Here is how the portfolio is moving today." action={<Button>Open lead desk <ArrowRight size={14} /></Button>} /><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-6"><MetricCard label="New leads" value={String(dashboard.newLeads)} note="since yesterday" icon={Users} /><MetricCard label="Qualified" value={String(dashboard.qualified)} note="ready for human follow-up" icon={Target} accent /><MetricCard label="Appointments" value={String(dashboard.appointments)} note="next 7 days" icon={CalendarDays} /><MetricCard label="Pipeline value" value={zar(dashboard.pipelineValue)} note="active opportunities" icon={TrendingUp} /><MetricCard label="Revenue won" value={zar(dashboard.revenueWon)} note="this financial year" icon={BarChart3} /><MetricCard label="Follow-ups due" value={String(dashboard.followUpsDue)} note="needs attention today" icon={Clock3} /></div><div className="mt-6 grid gap-5 xl:grid-cols-[1.2fr_.8fr]"><section className="rounded-sm border border-border bg-card p-5 md:p-6"><div className="mb-7 flex items-start justify-between"><div><div className="font-mono text-[10px] uppercase tracking-[.18em] text-muted-foreground">Funnel health</div><h2 className="mt-1 font-display text-lg font-bold">Where value is sitting</h2></div><span className="text-xs font-semibold text-accent">View reports <ArrowRight className="ml-1 inline" size={13} /></span></div><div className="space-y-4">{dashboard.stageCounts.map((stage, i) => <div className="grid grid-cols-[112px_1fr_42px] items-center gap-3" key={stage.stage}><span className="truncate text-xs text-muted-foreground">{stage.stage}</span><div className="h-2 overflow-hidden rounded-full bg-secondary"><div className={`h-full rounded-full ${i === 2 ? 'bg-primary' : 'bg-accent/70'}`} style={{ width: `${Math.max(5, stage.count / max * 100)}%` }} /></div><span className="font-mono text-right text-xs">{stage.count}</span></div>)}</div><div className="mt-8 grid grid-cols-3 gap-3 border-t border-border pt-5"><div><div className="font-mono text-[10px] text-muted-foreground">QUALIFICATION RATE</div><div className="mt-1 font-display text-lg font-bold">67%</div></div><div><div className="font-mono text-[10px] text-muted-foreground">BOOKING RATE</div><div className="mt-1 font-display text-lg font-bold">63%</div></div><div><div className="font-mono text-[10px] text-muted-foreground">ACTIVE TOUCHES</div><div className="mt-1 font-display text-lg font-bold">7</div></div></div></section><section className="rounded-sm border border-border bg-card p-5 md:p-6"><div className="mb-6"><div className="font-mono text-[10px] uppercase tracking-[.18em] text-muted-foreground">Coming up</div><h2 className="mt-1 font-display text-lg font-bold">Appointments</h2></div><div className="space-y-1">{dashboard.upcomingAppointments.map((item) => <AppointmentRow key={item.id} item={item} compact />)}</div></section></div></div></div></Shell>;
}
function zar(value: number) { return new Intl.NumberFormat('en-ZA', { style: 'currency', currency: 'ZAR', maximumFractionDigits: 0 }).format(value); }