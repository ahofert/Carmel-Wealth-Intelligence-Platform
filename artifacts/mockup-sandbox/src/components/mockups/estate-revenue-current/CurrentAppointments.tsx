import './_group.css';
import { CalendarDays, Plus } from 'lucide-react';
import { appointments, AppointmentRow, Button, Empty, Head, leads, Shell } from './_shared';

export function CurrentAppointments() {
  const upcoming = appointments.filter((a) => a.status !== 'completed');
  return <Shell active="Appointments"><div className="min-h-[calc(100vh-76px)] px-5 py-8 md:px-9 md:py-10"><div className="mx-auto max-w-[1200px]"><Head eyebrow="Revenue desk / calendar" title="Appointments" detail="Protect the handoff from intent to in-person. Every booking has an owner, place and next reminder." action={<Button><Plus size={15} />Book appointment</Button>} /><div className="grid gap-5 lg:grid-cols-[1fr_330px]"><section className="rounded-sm border border-border bg-card p-5 md:p-6"><div className="mb-5 flex items-center justify-between"><div><div className="font-mono text-[10px] uppercase tracking-[.18em] text-muted-foreground">Next 7 days</div><h2 className="mt-1 font-display text-lg font-bold">{upcoming.length} booked appointments</h2></div><div className="flex items-center gap-1.5 text-xs text-accent"><span className="h-2 w-2 rounded-full bg-primary" />Reminders monitored</div></div>{upcoming.map((item) => <AppointmentRow item={item} key={item.id} />)}<div className="mt-8 border-t border-border pt-5"><div className="mb-3 font-mono text-[10px] uppercase tracking-[.18em] text-muted-foreground">Recently completed</div><AppointmentRow item={appointments[2]} /></div></section><aside className="space-y-4"><div className="rounded-sm border border-border bg-foreground p-5 text-background"><div className="flex items-center justify-between"><CalendarDays size={18} className="text-primary" /><span className="font-mono text-[10px] uppercase tracking-wider text-background/50">Booking hygiene</span></div><div className="mt-6 font-display text-4xl font-bold">100%</div><p className="mt-1 text-xs text-background/65">of upcoming appointments have a logged location</p><div className="mt-5 border-t border-background/15 pt-4 text-[11px] text-background/60">Outbound reminders are not connected. Use the activity timeline to record calls and confirmations.</div></div><div className="rounded-sm border border-border bg-card p-5"><div className="font-mono text-[10px] uppercase tracking-[.18em] text-muted-foreground">Appointment types</div><div className="mt-4 space-y-3">{['Viewing', 'Valuation', 'Developer meeting'].map((type) => <div className="flex justify-between text-xs" key={type}><span>{type}</span><span className="font-mono text-muted-foreground">{appointments.filter((a) => a.type === type).length}</span></div>)}</div></div></aside></div></div></div></Shell>;
}