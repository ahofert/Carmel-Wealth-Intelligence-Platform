import './_group.css';
import { CircleCheck, Mail, Phone, SlidersHorizontal, ToggleLeft } from 'lucide-react';
import { automations, Button, Empty, Head, Shell } from './_shared';

export function CurrentAutomations() {
  return <Shell active="Automations"><div className="min-h-[calc(100vh-76px)] px-5 py-8 md:px-9 md:py-10"><div className="mx-auto max-w-[1200px]"><Head eyebrow="Revenue desk / orchestration" title="Automations" detail="Consent-aware plays for the moments that usually leak revenue. Review state before anything leaves the workspace." /><div className="mb-5 flex items-center gap-3 rounded-sm border border-primary/30 bg-[#f0f5d8] p-4 text-xs"><CircleCheck size={17} className="shrink-0 text-accent" /><span><strong>Delivery guardrail:</strong> outbound SMS and email integrations are not connected. Active means the workflow is ready, not that a message was sent.</span></div><div className="grid gap-4">{automations.map((item) => <div className="grid gap-4 rounded-sm border border-border bg-card p-5 md:grid-cols-[1fr_160px_130px_96px] md:items-center" key={item.id}><div><div className="flex items-center gap-2"><span className={`h-2 w-2 rounded-full ${item.active ? 'bg-primary' : 'bg-muted-foreground/40'}`} /><h2 className="font-display text-base font-bold">{item.name}</h2></div><p className="mt-1 max-w-2xl text-xs leading-relaxed text-muted-foreground">{item.description}</p><div className="mt-3 flex flex-wrap gap-2"><span className="rounded-full bg-secondary px-2 py-1 font-mono text-[9px] uppercase tracking-wider">{item.category}</span><span className="rounded-full bg-secondary px-2 py-1 font-mono text-[9px] uppercase tracking-wider">{item.trigger}</span></div></div><div><div className="font-mono text-[9px] uppercase text-muted-foreground">Channel</div><div className="mt-1 flex items-center gap-1.5 text-xs font-medium">{item.channel === 'SMS' ? <Phone size={13} /> : <Mail size={13} />}{item.channel}</div></div><div><div className="font-mono text-[9px] uppercase text-muted-foreground">Activity</div><div className="mt-1 font-display text-lg font-bold">{item.sentCount}</div><div className="text-[10px] text-muted-foreground">attempts logged</div></div><Button variant={item.active ? 'primary' : 'outline'}><ToggleLeft size={14} />{item.active ? 'Active' : 'Paused'}</Button></div>)}</div></div></div></Shell>;
}