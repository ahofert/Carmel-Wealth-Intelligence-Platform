import './_group.css';
import { ArrowRight, BarChart3, RefreshCw, TrendingUp, Users } from 'lucide-react';
import { Button, Head, MetricCard, reports, Shell, zar } from './_shared';

export function CurrentReports() {
  const maxRevenue = Math.max(...reports.monthlyRevenue.map((m) => m.revenue));
  const maxSource = Math.max(...reports.leadSources.map((s) => s.leads));
  return <Shell active="Reports"><div className="min-h-[calc(100vh-76px)] px-5 py-8 md:px-9 md:py-10"><div className="mx-auto max-w-[1300px]"><Head eyebrow="Revenue desk / intelligence" title="Reports" detail="Source-to-close performance across the operating portfolio, in rand." action={<Button variant="outline"><RefreshCw size={14} />Refresh data</Button>} /><div className="grid gap-3 sm:grid-cols-3"><MetricCard label="Total leads" value={String(reports.totalLeads)} note="all captured conversations" icon={Users} /><MetricCard label="Conversion rate" value={`${reports.conversionRate}%`} note="lead to won" icon={TrendingUp} accent /><MetricCard label="Reporting period" value="Apr 2026" note="latest complete month" icon={BarChart3} /></div><div className="mt-5 grid gap-5 lg:grid-cols-[1.2fr_.8fr]"><section className="rounded-sm border border-border bg-card p-5 md:p-6"><div className="mb-6"><div className="font-mono text-[10px] uppercase tracking-[.18em] text-muted-foreground">Monthly revenue</div><h2 className="mt-1 font-display text-lg font-bold">Won value by month</h2></div><div className="flex h-[220px] items-end gap-2 border-b border-border sm:gap-4">{reports.monthlyRevenue.map((month) => <div className="flex min-w-0 flex-1 flex-col items-center justify-end gap-2" key={month.month}><div className="font-mono text-[9px] text-muted-foreground">{zar(month.revenue)}</div><div className="w-full max-w-[52px] rounded-t-sm bg-accent" style={{ height: `${Math.max(8, month.revenue / maxRevenue * 150)}px` }} /><div className="font-mono text-[9px] text-muted-foreground">{month.month.slice(0, 3)}</div></div>)}</div></section><section className="rounded-sm border border-border bg-card p-5 md:p-6"><div className="mb-6"><div className="font-mono text-[10px] uppercase tracking-[.18em] text-muted-foreground">Lead sources</div><h2 className="mt-1 font-display text-lg font-bold">What is creating demand</h2></div><div className="space-y-5">{reports.leadSources.map((source, i) => <div key={source.source}><div className="mb-2 flex justify-between text-xs"><span className="font-medium">{source.source}</span><span className="font-mono text-muted-foreground">{source.leads}</span></div><div className="h-2 overflow-hidden rounded-full bg-secondary"><div className={`h-full rounded-full ${i === 0 ? 'bg-primary' : 'bg-accent/70'}`} style={{ width: `${source.leads / maxSource * 100}%` }} /></div></div>)}</div></section></div><section className="mt-5 rounded-sm border border-border bg-card p-5 md:p-6"><div className="mb-6 flex items-end justify-between"><div><div className="font-mono text-[10px] uppercase tracking-[.18em] text-muted-foreground">Pipeline conversion</div><h2 className="mt-1 font-display text-lg font-bold">Stage movement</h2></div><span className="font-mono text-[10px] text-muted-foreground">LEAD COUNT</span></div><div className="grid gap-3 md:grid-cols-5">{reports.funnel.map((stage, i) => <div className="relative border-l-2 border-accent/30 pl-4" key={stage.stage}><div className="font-mono text-[10px] uppercase text-muted-foreground">{stage.stage}</div><div className="mt-2 font-display text-3xl font-bold">{stage.leads}</div>{i < reports.funnel.length - 1 && <ArrowRight className="absolute -right-3 top-7 hidden text-border md:block" size={16} />}</div>)}</div></section></div></div></Shell>;
}